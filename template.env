# Database Configuration
POSTGRES_DB=mcp_series
POSTGRES_USER=your_db_user
POSTGRES_PASSWORD=your_secure_password
# Container name for PostgreSQL (must match docker-compose.yml)
POSTGRES_CONTAINER_NAME=mcp-series-db

# Connection URL for the application (must match the database settings above)
DATABASE_URL=postgresql://your_db_user:your_secure_password@localhost:5432/mcp_series
#use this string to connect to your db
#postgresql://your_db_user:@localhost:5432/mcp_series

# Node Environment
NODE_ENV=development

# MCP Configuration Settings
# Path to the MCP tutorial project (leave blank to use current directory)
MCP_TUTORIAL_PATH=

# HTTP server settings for Typing Mind integration
MCP_SERVER_HOST=localhost
MCP_SERVER_PORT=3500
# Typing Mind Port

# Note: Replace all values with your actual secure credentials
# Do not commit your actual .env file to version control
# Copy this template to .env and update the values
